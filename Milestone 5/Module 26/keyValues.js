const king = {
    name: 'Mustafa',
    age: 55,
    kingdom: 'Prid-Lands',
}

const keys= Object.keys(king);
const values = Object.values(king);
const entires = Object.entries(king);
console.log(entires);